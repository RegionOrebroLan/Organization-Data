{
	"ConnectionStrings": {
		// It seems that Sqlite uses AppDomain.CurrentDomain.BaseDirectory to resolve relative paths. Thats why the data-source path below.
		"Sqlite": "Data Source=../../../Data/Organization.db",
		"SqlServer": "Server=(LocalDB)\\MSSQLLocalDB;AttachDbFileName=Data/Organization.mdf;Integrated Security=True"
	}
}